/**
 * 预览缩放 —— 实时预览与「优化简历」页共用的全局缩放级别。
 *
 * 两个页面渲染的是同一份简历，必须用同一个缩放值：
 * 否则会出现"优化简历页的字比实时预览大"这种不一致（两边布局宽度还一样才怪）。
 * 这里用模块级 ref 保存当前缩放，谁改都全局生效。
 */
import { ref } from 'vue'

/** 当前预览缩放（1 = 100%） */
export const previewZoom = ref(0.8)

/** 可选的缩放档位 */
export const ZOOM_LEVELS = [0.6, 0.8, 1] as const
